{
  "name": "www",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "devDependencies": {
    "esbuild": "^0.19.5",
    "typescript": "^5.2.2"
  },
  "type": "module",
  "dependencies": {
    "@bjorn3/browser_wasi_shim": "^0.2.15"
  },
  "scripts": {
    "build": "esbuild --sourcemap --outdir=dist --loader:.wasm=file --bundle --format=esm index.ts && cp index.html dist/"
  }

}
